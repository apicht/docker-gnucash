# GnuCash in a Docker image

## Compose Usage

### Configure

- Copy `.env.example` to `.env` and set `POSTGRES_PASSWORD`

### Run

```shell
docker compose up -d
```

### Access

http://localhost:3000

### Stop

```shell
docker compose down
```
